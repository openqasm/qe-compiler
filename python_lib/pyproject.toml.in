[build-system]
requires = [
    "setuptools>=40.6.0",
    "wheel",
    "setuptools_scm[toml]>=6.0",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "${CMAKE_CURRENT_BINARY_DIR}/qss_compiler/_version.py"
root = "${CMAKE_SOURCE_DIR}"
fallback_version = "${VERSION_STRING}"
